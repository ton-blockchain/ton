diff --git a/CMakeLists.txt b/CMakeLists.txt
index 62e64b39..6815f7a3 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -169,14 +169,25 @@ if(OPENSSL_FOUND)
     cmake_push_check_state()
     set(CMAKE_REQUIRED_INCLUDES   "${OPENSSL_INCLUDE_DIR}")
     set(CMAKE_REQUIRED_LIBRARIES  "${OPENSSL_LIBRARIES}")
+    if(MINGW AND OPENSSL_SSL_LIBRARY AND OPENSSL_CRYPTO_LIBRARY)
+      # Prefer explicit static OpenSSL libs to avoid check_symbol_exists link failures.
+      set(CMAKE_REQUIRED_LIBRARIES
+        "${OPENSSL_SSL_LIBRARY};${OPENSSL_CRYPTO_LIBRARY};${CMAKE_REQUIRED_LIBRARIES}")
+    endif()
     if(WIN32)
-      set(CMAKE_REQUIRED_LIBRARIES "${CMAKE_REQUIRED_LIBRARIES}" "ws2_32" "bcrypt" "crypt32")
+      set(CMAKE_REQUIRED_LIBRARIES "${CMAKE_REQUIRED_LIBRARIES}" "ws2_32" "bcrypt" "crypt32"
+        "advapi32" "user32" "gdi32" "kernel32")
     endif()
     check_symbol_exists(SSL_provide_quic_data "openssl/ssl.h" HAVE_SSL_PROVIDE_QUIC_DATA)
     if(NOT HAVE_SSL_PROVIDE_QUIC_DATA)
       check_symbol_exists(SSL_set_quic_tls_cbs "openssl/ssl.h" HAVE_SSL_SET_QUIC_TLS_CBS)
       if(NOT HAVE_SSL_SET_QUIC_TLS_CBS)
-        message(FATAL_ERROR "Unable to build OpenSSL backend due to lack of QUIC support in ${OPENSSL_LIBRARIES}")
+        if(MINGW)
+          # MinGW static OpenSSL checks can fail at link time; assume QUIC support if using our custom OpenSSL.
+          set(HAVE_SSL_SET_QUIC_TLS_CBS TRUE)
+        else()
+          message(FATAL_ERROR "Unable to build OpenSSL backend due to lack of QUIC support in ${OPENSSL_LIBRARIES}")
+        endif()
       endif()
     else()
       check_symbol_exists("LIBRESSL_VERSION_NUMBER" "openssl/opensslv.h" HAVE_LIBRESSL_VERSION_NUMBER)
