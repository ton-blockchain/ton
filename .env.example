# Example env file (DO NOT commit private keys)
RPC_URL=https://net.ton.dev
SECRET_KEY_BASE64= # base64-encoded secret key (or use a secret manager)
WORKCHAIN=0
SUBWALLET_ID=0x10ad
TIMEOUT=3600
# Optional: set DEPLOY_VALUE to override estimator (in TON)
DEPLOY_VALUE=
# Safety and estimator tuning
DEPLOY_SAFE_MARGIN=1.5
DEPLOY_FALLBACK=0.1
# To actually perform deploy set CONFIRM_DEPLOY=true in environment (or secret)
CONFIRM_DEPLOY=false
