[project]
name = "tontester"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.4",
    "pytonlib>=0.0.68",
    "setuptools<81", # FIXME: Get rid of pytonlib instead.
]
