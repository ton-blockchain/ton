[project]
name = "tontester"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "dash",
    "plotly",
    "pytoniq-core",
    "watchfiles",
    "requests",
]

[project.scripts]
consensus_explorer = "consensus_explorer.explorer:_main"
