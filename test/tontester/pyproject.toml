[project]
name = "tontester"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "dash",
    "plotly",
    "watchfiles>=1.1.1",
    "pytoniq_core>=0.1.46"
]

[project.scripts]
consensus_explorer = "consensus_explorer.explorer:_main"
